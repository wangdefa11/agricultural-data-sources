# 归档说明

这里保存已经停用、但暂时不直接删除的历史方案。

- `legacy-web-dashboard/`：早期网页/JavaScript 图表方案。
- `legacy-outputs/`：早期任务生成的预览图、检查文件、旧版工作簿和课件复刻文件。

当前项目的正式代码以根目录下的 `soybean_report/` 和 `scripts/*.py`
为准；正式交付文件统一位于 `outputs/`。
