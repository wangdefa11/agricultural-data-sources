[project]
name = "soybean-report"
version = "0.1.0"
description = "USDA soybean fundamentals database and macro report generator"
requires-python = ">=3.11"

[project.scripts]
soybean-report = "soybean_report.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
